name: fibernet_analytics
version: "1.0.0"
config-version: 2

profile: fibernet

model-paths:  ["models"]
test-paths:   ["tests"]
seed-paths:   ["seeds"]
macro-paths:  ["macros"]
analysis-paths: ["analyses"]

target-path:  "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fibernet_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
